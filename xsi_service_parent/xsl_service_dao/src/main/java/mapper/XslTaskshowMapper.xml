<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mapper.XslTaskshowMapper">
    <!-- namespace接口权限命名 -->
    <select id="getXslTaskList" resultType="pojo.XslTask">
        SELECT
        id,cid,descr,sendId,money,state,createDate,updateDate,deadline from xsl_task
        <where>
            <if test="queryText!=null">
                and name like concat('%',#{queryText},'%')
            </if>
            id>0 ORDER BY id limit #{start},#{size}
        </where>
    </select>
    <select id="getXslTaskCount" resultType="java.lang.Integer">
        select
        count(*)
        from xsl_task
        <where>
            <if test="queryText != null">
                and name like concat('%', #{queryText}, '%')
            </if>
        </where>
    </select>

    <select id="getXslTaskListC" resultType="pojo.XslTaskPosh">
        select
        xsl_task.id,xsl_task.cid,xsl_task.descr,xsl_task.sendId,xsl_task.money,xsl_task.state,xsl_task.createDate,xsl_task.updateDate,xsl_task.deadline,xsl_user.`name`
        from xsl_task,xsl_user
        <where>
            xsl_user.id=xsl_task.cid
            and
            xsl_task.id>0 ORDER BY xsl_task.id limit #{start},#{size}
        </where>
    </select>

    <select id="getXslTaskCountC" resultType="java.lang.Integer">
        select
        count(*)
        from xsl_task,xsl_user
        <where>
            xsl_user.id=xsl_task.cid
        </where>
    </select>
</mapper>